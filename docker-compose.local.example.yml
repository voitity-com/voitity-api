services:
  queue:
    command:
      - php
      - artisan
      - queue:work
      - database
      - --queue=chat,media,default
      - --sleep=1
      - --tries=3
      - --timeout=300
      - --max-time=3600
